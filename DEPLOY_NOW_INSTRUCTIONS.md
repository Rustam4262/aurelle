# 🚀 Быстрый деплой на aurelle.uz

## ⚡ Для запуска деплоя нужны данные сервера

Скрипт запросил учетные данные. Вам нужно:

### Вариант 1: Создать .env.deploy файл

```powershell
# Создайте файл .env.deploy
@"
DEPLOY_SERVER_IP=ВАШ_IP_СЕРВЕРА
DEPLOY_SERVER_USER=root
DEPLOY_SERVER_PATH=/var/www/beauty_salon
"@ | Out-File -FilePath .env.deploy -Encoding utf8

# Замените ВАШ_IP_СЕРВЕРА на реальный IP адрес сервера aurelle.uz
```

Затем запустите:
```powershell
.\deploy-to-prod-from-git.ps1
```

### Вариант 2: Ввести данные при запросе

Просто запустите скрипт и введите данные когда он попросит:
```powershell
.\deploy-to-prod-from-git.ps1

# Введите:
# - Server IP address: (IP вашего сервера)
# - SSH username: root (или нажмите Enter)
# - Project path: /var/www/beauty_salon (или нажмите Enter)
```

---

## 📋 Что будет сделано

После ввода данных скрипт автоматически:

1. ✅ Подключится к серверу aurelle.uz
2. ✅ Выполнит `git pull` на сервере (получит все ваши изменения из GitHub)
3. ✅ Остановит текущие контейнеры
4. ✅ Пересоберет Docker образы с новым кодом
5. ✅ Запустит обновленные контейнеры
6. ✅ Выполнит миграции БД (если есть)
7. ✅ Проверит что все работает

**Результат:** Все изменения из GitHub будут на продакшене aurelle.uz! 🎉

---

## ⚠️ Важно

- Убедитесь что изменения закоммичены и запушены в GitHub
- Убедитесь что на сервере настроен Git репозиторий
- Убедитесь что у вас есть SSH доступ к серверу

---

**Готово к запуску!** Просто введите данные сервера когда скрипт попросит.

