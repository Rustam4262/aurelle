# 🚀 ДЕПЛОЙ СЕЙЧАС - Быстрая инструкция

## ⚡ TL;DR - Обновить продакшен СЕЙЧАС

```bash
ssh root@89.39.94.194
cd ~/projects/aurelle
git pull
bash ./deploy/scripts/update.sh
```

**Вот и всё!** Остальное произойдёт автоматически.

---

## 📋 Что произойдёт автоматически

Скрипт `update.sh` выполнит **8 шагов:**

1. ✅ **Валидация окружения** - проверит все переменные
2. ✅ **Бэкап базы данных** - сохранит в `backups/`
3. ✅ **Git pull** - скачает изменения
4. ✅ **Пересборка Docker** - обновит образы
5. ✅ **Перезапуск** - запустит новые контейнеры
6. ✅ **Миграции БД** - применит изменения схемы
7. ✅ **Очистка** - удалит мусор
8. ✅ **Проверка здоровья** - проверит что всё работает
9. ✅ **Уведомление** - отправит результат в Telegram

---

## ⏱️ Сколько времени

- **Подключение:** 10 секунд
- **Git pull:** 5 секунд
- **Деплой скрипт:** 2-3 минуты
- **Проверка:** 30 секунд

**Итого: ~4 минуты** от начала до конца.

---

## 📱 После деплоя

### 1. Проверьте сайт (30 секунд)

Откройте в браузере:
- ✅ https://aurelle.uz - должен открыться
- ✅ https://api.aurelle.uz/health - должно быть `{"status":"ok"}`
- ✅ https://api.aurelle.uz/docs - API документация

### 2. Проверьте здоровье (опционально)

```bash
bash ./deploy/scripts/check_health.sh
```

Должно быть всё зелёное ✅

### 3. Проверьте Telegram

Если настроили Telegram бота, должно прийти уведомление:
> ✅ AURELLE обновлён успешно! 2025-12-09 17:30

---

## 🆘 Если что-то пошло не так

### Проблема: Скрипт остановился с ошибкой

```bash
# Посмотрите что пошло не так в логах
docker-compose -f docker-compose.prod.yml logs --tail=50

# Попробуйте перезапустить вручную
docker-compose -f docker-compose.prod.yml restart
```

### Проблема: Сайт не открывается после деплоя

```bash
# Проверьте статус контейнеров
docker-compose -f docker-compose.prod.yml ps

# Если контейнер не запущен, посмотрите логи
docker-compose -f docker-compose.prod.yml logs frontend
docker-compose -f docker-compose.prod.yml logs backend

# Перезапустите
docker-compose -f docker-compose.prod.yml restart
```

### Проблема: Нужно откатиться назад (Rollback)

```bash
# 1. Вернитесь на предыдущий коммит
git log --oneline -5  # посмотрите последние коммиты
git checkout <предыдущий-коммит>

# 2. Пересоберите
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d --build

# 3. Или восстановите из бэкапа
ls -lt backups/  # найдите последний бэкап
bash ./deploy/scripts/restore.sh backups/aurelle_YYYYMMDD_HHMMSS.sql.gz
```

---

## 📞 Контакты для помощи

Если ничего не помогает:

1. **Посмотрите документацию:**
   - [DEPLOYMENT_READY.md](DEPLOYMENT_READY.md) - полный гайд
   - [SUMMARY_FOR_OWNER.md](SUMMARY_FOR_OWNER.md) - краткая сводка
   - [TROUBLESHOOTING](SUMMARY_FOR_OWNER.md#-troubleshooting) - решение проблем

2. **Проверьте логи:**
   ```bash
   docker-compose -f docker-compose.prod.yml logs
   ```

3. **Проверьте переменные окружения:**
   ```bash
   bash ./deploy/scripts/validate_env.sh
   ```

---

## ✅ Чеклист перед деплоем

Быстрая проверка (30 секунд):

- [ ] Есть доступ к серверу (89.39.94.194)
- [ ] Файл `.env` настроен на сервере
- [ ] Есть свежий бэкап (делается автоматически)
- [ ] Git репозиторий в порядке (нет незакоммиченных изменений на сервере)

Всё ✅ ? Тогда вперёд:

```bash
bash ./deploy/scripts/update.sh
```

---

## 🎉 После успешного деплоя

### Что изменилось в этом деплое:

1. ✅ **Убран localhost из фронтенда**
   - Frontend теперь правильно работает на https://aurelle.uz
   - API URL настраивается через переменные окружения

2. ✅ **Исправлена сборка**
   - `npm run build` больше не падает
   - TypeScript не блокирует деплой

3. ✅ **Добавлен мониторинг**
   - Автоматическая проверка переменных окружения
   - Health check для всех сервисов
   - Telegram уведомления (если настроили)

4. ✅ **Улучшена безопасность**
   - Все `.env` исключены из git
   - Бэкапы не попадают в репозиторий
   - Автоматическая валидация настроек

5. ✅ **Создана документация**
   - Полные гайды по деплою
   - Roadmap развития функционала
   - Список улучшений

### Что дальше:

1. **Сейчас (5 минут):**
   - Проверьте что сайт работает
   - Зарегистрируйтесь / войдите
   - Создайте тестовый салон

2. **Сегодня (20 минут):**
   - Настройте Telegram бота для уведомлений
   - Настройте UptimeRobot для мониторинга
   - Добавьте cron job для автоматической проверки

3. **На этой неделе:**
   - Добавьте реальные салоны
   - Начните продвижение
   - Соберите первую обратную связь

4. **Следующие 2-3 недели (Version 1.1):**
   - Реализуйте чат мессенджер
   - Добавьте социальный вход (Google, Telegram)
   - Внедрите функционал выезда на дом

См. [ROADMAP.md](ROADMAP.md) для полного плана развития.

---

## 📊 Полезные команды

### На продакшене:

```bash
# Проверить статус
docker-compose -f docker-compose.prod.yml ps

# Посмотреть логи (последние 50 строк)
docker-compose -f docker-compose.prod.yml logs --tail=50

# Посмотреть логи конкретного сервиса
docker-compose -f docker-compose.prod.yml logs frontend --tail=50
docker-compose -f docker-compose.prod.yml logs backend --tail=50

# Перезапустить сервис
docker-compose -f docker-compose.prod.yml restart frontend
docker-compose -f docker-compose.prod.yml restart backend

# Проверить здоровье
bash ./deploy/scripts/check_health.sh

# Сделать бэкап вручную
bash ./deploy/scripts/backup.sh

# Проверить переменные окружения
bash ./deploy/scripts/validate_env.sh

# Посмотреть использование ресурсов
docker stats

# Посмотреть использование диска
df -h

# Посмотреть бэкапы
ls -lh backups/
```

---

## 🎯 Главное

**Для обновления продакшена достаточно ОДНОЙ команды:**

```bash
bash ./deploy/scripts/update.sh
```

Всё остальное произойдёт автоматически! 🚀

---

**Удачного деплоя! 🎊**

Если возникнут вопросы - вся документация в корне проекта.
