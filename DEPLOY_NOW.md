# 🚀 Деплой на aurelle.uz - Инструкция

## ✅ Да, скрипт обновит всё на aurelle.uz!

После запуска скрипта все изменения из GitHub будут:
- ✅ Загружены на сервер (`git pull`)
- ✅ Пересобраны в Docker образах
- ✅ Развернуты на продакшене
- ✅ Доступны на https://aurelle.uz

---

## 📋 Что нужно сделать СЕЙЧАС

### Шаг 1: Узнать IP адрес сервера

У вас есть два варианта:

**Вариант А:** Если знаете IP адрес сервера:
- Создайте файл `.env.deploy` (см. ниже)

**Вариант Б:** Если не знаете IP:
- Запустите скрипт и введите данные когда он попросит

### Шаг 2: Создать .env.deploy (если знаете IP)

```powershell
# Создайте файл .env.deploy
@"
DEPLOY_SERVER_IP=ВАШ_IP_АДРЕС_СЕРВЕРА
DEPLOY_SERVER_USER=root
DEPLOY_SERVER_PATH=/var/www/beauty_salon
"@ | Out-File -FilePath .env.deploy -Encoding utf8

# Замените ВАШ_IP_АДРЕС_СЕРВЕРА на реальный IP
```

### Шаг 3: Запустить скрипт

```powershell
.\deploy-to-prod-from-git.ps1
```

---

## 🔍 Как узнать IP адрес сервера?

Если у вас есть доступ к серверу, можно узнать IP:

```bash
# На сервере
hostname -I
# или
ip addr show
```

Или спросите у администратора сервера / хостинг-провайдера.

---

## ⚡ Быстрый запуск (если знаете IP)

Просто скажите мне IP адрес сервера, и я создам `.env.deploy` и запущу скрипт!

Или введите IP адрес сервера для aurelle.uz, и я продолжу деплой.

