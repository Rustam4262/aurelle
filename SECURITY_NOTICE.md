# ⚠️ ВАЖНО: Безопасность учетных данных

## Обнаруженная проблема

В предыдущих версиях проекта учетные данные продакшн сервера были захардкожены в файлах документации и скриптах деплоя. Это серьезная проблема безопасности.

## ✅ Что было исправлено

1. ✅ Все учетные данные удалены из файлов проекта
2. ✅ Создан `.gitignore` для исключения чувствительных файлов
3. ✅ Создан шаблон `.env.deploy.example` без реальных данных
4. ✅ Все скрипты деплоя теперь читают данные из `.env.deploy`
5. ✅ Документация обновлена с инструкциями по безопасной настройке

## 🔐 Что нужно сделать СЕЙЧАС

### 1. Если учетные данные уже в git истории:

**КРИТИЧЕСКИ ВАЖНО:** Если репозиторий публичный или был скомпрометирован:

1. **СРОЧНО смените пароли на сервере:**
   ```bash
   # На сервере
   passwd root  # Смените пароль root
   ```

2. **Смените пароль базы данных:**
   ```sql
   ALTER USER aurelleu_aurelle_user WITH PASSWORD 'новый_безопасный_пароль';
   ```

3. **Если используете SSH ключи - отзовите старые и создайте новые**

4. **Проверьте логи на сервере на предмет несанкционированного доступа**

### 2. Настройте безопасное хранение учетных данных:

```bash
# 1. Создайте файл с учетными данными (НЕ коммитьте в git!)
cp .env.deploy.example .env.deploy

# 2. Заполните реальные данные
nano .env.deploy

# 3. Убедитесь что файл в .gitignore
grep .env.deploy .gitignore
```

### 3. Используйте SSH ключи вместо паролей:

```bash
# Генерируйте SSH ключ (если нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопируйте на сервер
ssh-copy-id YOUR_USER@YOUR_SERVER_IP

# Теперь можно подключаться без пароля
ssh YOUR_USER@YOUR_SERVER_IP
```

## 📋 Чеклист безопасности

- [ ] `.env.deploy` добавлен в `.gitignore` ✅
- [ ] Все скрипты читают из `.env.deploy` ✅
- [ ] Документация обновлена ✅
- [ ] Пароли на сервере изменены (если репозиторий был публичным)
- [ ] SSH ключи настроены
- [ ] Пароль БД изменен (если был скомпрометирован)
- [ ] Логи сервера проверены

## 🚫 Что НИКОГДА не делать

- ❌ НЕ коммитить `.env.deploy` в git
- ❌ НЕ хранить пароли в коде
- ❌ НЕ делиться учетными данными в документации
- ❌ НЕ использовать слабые пароли
- ❌ НЕ использовать один пароль для всего

## ✅ Рекомендации

1. **Используйте менеджер паролей** (1Password, LastPass, Bitwarden)
2. **Настройте SSH ключи** вместо паролей
3. **Используйте разные пароли** для разных сервисов
4. **Регулярно меняйте пароли** (каждые 3-6 месяцев)
5. **Включите двухфакторную аутентификацию** где возможно
6. **Мониторьте логи** на подозрительную активность

---

**Дата исправления:** 2025-01-27  
**Статус:** ✅ Исправлено

