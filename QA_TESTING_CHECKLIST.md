# 🧨 AURELLE – QA HELL MODE CHECKLIST

**Дата**: 10 декабря 2025
**Версия**: 1.0.0
**Цель**: Сломать всё, что разработчики "аккуратно обошли"

---

## 📋 ОБЩИЕ ПРАВИЛА

### Задача QA:
Найти **все** способы сломать систему до того, как это сделают пользователи.

### Любая фича = проверка:
- ✅ **Happy path** - всё работает как надо
- 🤪 **Кривые руки пользователя** - случайные ошибки
- 😈 **Злой хакер** - намеренная атака
- 📡 **Плохой интернет** - нестабильное соединение

### Любой баг → репорт:
```
Шаги воспроизведения:
1. ...
2. ...

Ожидаемый результат: ...
Фактический результат: ...

Логи / Скрин / Видео: [ссылка]
Приоритет: P0 / P1 / P2 / P3
```

### Приоритеты багов:
- 🔴 **P0** (Blocker) - Система не работает, деньги теряются, данные утекают
- 🟠 **P1** (Critical) - Основная функциональность сломана
- 🟡 **P2** (Major) - Неудобно, но работает
- 🟢 **P3** (Minor) - Косметические дефекты

---

## 1️⃣ SMOKE-ТЕСТ (КАЖДЫЙ БИЛД)

**Цель**: Понять за **5 минут**, стоит ли вообще тратить время на этот билд.

### Быстрая проверка:

| Проверка | Статус | Примечание |
|----------|--------|------------|
| **Главная страница открывается?** | ⬜ | http://localhost:5173 |
| **Регистрация работает?** | ⬜ | Новый пользователь |
| **Логин работает?** | ⬜ | Существующий пользователь |
| **Выход работает?** | ⬜ | Logout |
| **Салон создаётся?** | ⬜ | Для владельца салона |
| **Мастер добавляется?** | ⬜ | |
| **Услуга создаётся?** | ⬜ | |
| **Запись создаётся?** | ⬜ | Клиент → Салон |
| **Запись отменяется?** | ⬜ | |
| **Личный кабинет клиента открывается?** | ⬜ | |
| **Панель салона открывается?** | ⬜ | |

**⚠️ Если хоть что-то НЕ работает → билд в мусор, разработчикам грусть.**

---

## 2️⃣ АВТОРИЗАЦИЯ, РОЛИ И ДОСТУПЫ

### Роли:
- 👤 **Гость** (не авторизован)
- 👥 **Клиент** (client)
- ✂️ **Мастер** (master)
- 🏢 **Владелец салона** (salon_owner)
- 👨‍💼 **Админ** (admin)

### 2.1. Регистрация

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| ✅ Валидные данные (email, пароль 8+ символов) | Успех, письмо/SMS | ⬜ |
| ❌ Email уже занят | Ошибка "Email already exists" | ⬜ |
| ❌ Телефон уже занят | Ошибка "Phone already exists" | ⬜ |
| ❌ Пароль < 8 символов | Ошибка "Password too short" | ⬜ |
| ❌ Пароль без цифр | Ошибка "Password must contain digit" | ⬜ |
| ❌ Пароль без заглавных букв | Ошибка "Password must contain uppercase" | ⬜ |
| ❌ Без галочки "согласен с условиями" | Ошибка "You must accept terms" | ⬜ |
| ❌ SQL injection в email (`' OR 1=1--`) | Безопасно игнорируется | ⬜ |
| ❌ XSS в имени (`<script>alert(1)</script>`) | Экранируется | ⬜ |

**Автоматизированный тест**:
```python
# backend/tests/test_auth_registration.py

def test_register_valid_user():
    response = client.post("/api/auth/register", json={
        "email": "newuser@test.com",
        "name": "New User",
        "password": "StrongPass123!",
        "phone": "+998901234567",
        "role": "client",
        "consents": [
            {"type": "terms", "version": "v1.0", "accepted": True},
            {"type": "privacy", "version": "v1.0", "accepted": True}
        ]
    })
    assert response.status_code == 200

def test_register_duplicate_email():
    # Первый раз - успех
    client.post("/api/auth/register", json={...})

    # Второй раз - ошибка
    response = client.post("/api/auth/register", json={...})
    assert response.status_code == 400
    assert "already exists" in response.json()["detail"].lower()

def test_register_weak_password():
    response = client.post("/api/auth/register", json={
        "password": "123"  # Слишком короткий
    })
    assert response.status_code == 400
    assert "password" in response.json()["detail"].lower()

def test_register_sql_injection():
    response = client.post("/api/auth/register", json={
        "email": "' OR 1=1--@test.com"
    })
    # Не должен создать пользователя
    assert response.status_code == 400

def test_register_xss():
    response = client.post("/api/auth/register", json={
        "name": "<script>alert('XSS')</script>"
    })
    # Должен создать, но экранировать
    assert response.status_code == 200
    user = response.json()
    assert "<script>" not in user["name"]
```

### 2.2. Логин

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| ✅ Верный email + пароль | Токен, успех | ⬜ |
| ❌ Верный email + неверный пароль | 401 Unauthorized | ⬜ |
| ❌ Несуществующий email | 401 Unauthorized | ⬜ |
| ❌ Заблокированный аккаунт | 403 Forbidden | ⬜ |
| ❌ 5+ неудачных попыток | Rate limit, временный бан | ⬜ |

**Автоматизированный тест**:
```python
# backend/tests/test_auth_login.py

def test_login_valid_credentials():
    response = client.post("/api/auth/login", json={
        "email": "test@example.com",
        "password": "TestPass123!"
    })
    assert response.status_code == 200
    assert "access_token" in response.json()
    assert "refresh_token" in response.json()

def test_login_wrong_password():
    response = client.post("/api/auth/login", json={
        "email": "test@example.com",
        "password": "WrongPassword"
    })
    assert response.status_code == 401

def test_login_rate_limit():
    # 5 неудачных попыток
    for _ in range(5):
        client.post("/api/auth/login", json={
            "email": "test@example.com",
            "password": "wrong"
        })

    # 6-я попытка - блокировка
    response = client.post("/api/auth/login", json={
        "email": "test@example.com",
        "password": "wrong"
    })
    assert response.status_code == 429  # Too Many Requests
```

### 2.3. Восстановление пароля

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| ✅ Существующий email | Письмо со ссылкой | ⬜ |
| ❌ Несуществующий email | Тихо игнорировать (безопасность) | ⬜ |
| ❌ 3+ запроса подряд | Rate limit | ⬜ |

### 2.4. Проверка ролей и доступов

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| Гость пытается открыть `/client/dashboard` | Редирект на `/login` | ⬜ |
| Клиент пытается открыть `/admin` | 403 Forbidden | ⬜ |
| Владелец салона пытается открыть `/admin` | 403 Forbidden | ⬜ |
| Админ открывает любую страницу | Доступ есть | ⬜ |
| Клиент пытается редактировать чужой профиль (API) | 403 Forbidden | ⬜ |
| Владелец пытается редактировать чужой салон (API) | 403 Forbidden | ⬜ |

**Автоматизированный тест**:
```python
# backend/tests/test_auth_permissions.py

def test_client_cannot_access_admin_panel():
    # Логинимся как клиент
    token = login_as_client()

    response = client.get("/api/admin/users", headers={
        "Authorization": f"Bearer {token}"
    })
    assert response.status_code == 403

def test_salon_owner_cannot_edit_other_salon():
    # Владелец салона 1
    token = login_as_salon_owner(salon_id=1)

    # Пытается редактировать салон 2
    response = client.patch("/api/salons/2", json={
        "name": "Hacked Salon"
    }, headers={"Authorization": f"Bearer {token}"})

    assert response.status_code == 403
```

---

## 3️⃣ ЗАПИСЬ (BOOKING) – СВЯТАЯ КОРОВА

**⚠️ КРИТИЧНО**: Недопустимы двойные записи и временные коллизии!

### 3.1. Базовая запись

| Шаг | Проверка | Статус |
|-----|----------|--------|
| 1 | Клиент выбирает салон | ⬜ |
| 2 | Выбирает мастера | ⬜ |
| 3 | Выбирает услугу | ⬜ |
| 4 | Выбирает дату и время | ⬜ |
| 5 | Подтверждает | ⬜ |
| 6 | Запись отображается у клиента | ⬜ |
| 7 | Запись отображается у салона/мастера | ⬜ |
| 8 | Уведомление клиенту (email/SMS) | ⬜ |
| 9 | Уведомление салону | ⬜ |

### 3.2. 🔥 Двойной клик

**Сценарий**: Пользователь два раза быстро жмёт "Записаться"

**Ожидаемый результат**: Создаётся **ОДНА** запись, не две!

**Как проверить**:
```
1. Открыть DevTools → Network → Slow 3G
2. Выбрать салон, мастера, услугу, время
3. БЫСТРО нажать "Записаться" 2 раза подряд
4. Проверить в БД: только 1 запись
```

**Автоматизированный тест**:
```python
# backend/tests/test_booking_double_click.py

import asyncio

@pytest.mark.asyncio
async def test_double_click_creates_one_booking():
    # Одновременные запросы
    tasks = [
        create_booking_async(user_id=1, slot_id=1),
        create_booking_async(user_id=1, slot_id=1)
    ]

    results = await asyncio.gather(*tasks, return_exceptions=True)

    # Только 1 успех
    successful = [r for r in results if not isinstance(r, Exception)]
    assert len(successful) == 1

    # Проверяем БД
    bookings = db.query(Booking).filter(
        Booking.user_id == 1,
        Booking.slot_id == 1
    ).all()
    assert len(bookings) == 1
```

### 3.3. 🔥 Два пользователя на один слот

**Сценарий**: Пользователь А и Б одновременно пытаются записаться на один и тот же слот

**Ожидаемый результат**:
- Один получает **успех**
- Второй получает **ошибку "слот занят"**

**Как проверить**:
```
1. Открыть два браузера (Chrome + Firefox)
2. Залогиниться как два разных пользователя
3. Оба выбирают ОДИН И ТОТ ЖЕ слот
4. Оба жмут "Записаться" ОДНОВРЕМЕННО
5. Проверить: только 1 запись в БД
```

**Автоматизированный тест**:
```python
# backend/tests/test_booking_race_condition.py

@pytest.mark.asyncio
async def test_concurrent_booking_same_slot():
    slot_data = {
        "salon_id": 1,
        "master_id": 1,
        "service_id": 1,
        "start_at": "2025-12-11T10:00:00Z"
    }

    # Два разных пользователя, один слот
    tasks = [
        create_booking_async(client_id=1, **slot_data),
        create_booking_async(client_id=2, **slot_data)
    ]

    results = await asyncio.gather(*tasks, return_exceptions=True)

    successful = [r for r in results if not isinstance(r, Exception)]
    failed = [r for r in results if isinstance(r, Exception)]

    assert len(successful) == 1
    assert len(failed) == 1
    assert "already booked" in str(failed[0]).lower()
```

**🛡️ Защита (должна быть реализована)**:
```python
# backend/app/api/bookings.py

from sqlalchemy import select, and_
from sqlalchemy.orm import with_for_update

@router.post("/")
def create_booking(booking_data: BookingCreate, db: Session = Depends(get_db)):
    # Database-level lock
    existing = db.execute(
        select(Booking)
        .filter(and_(
            Booking.master_id == booking_data.master_id,
            Booking.start_at == booking_data.start_at,
            Booking.status.in_(["pending", "confirmed"])
        ))
        .with_for_update()  # ← КРИТИЧНО!
    ).scalar_one_or_none()

    if existing:
        raise HTTPException(409, "Time slot already booked")

    # Создаём
    booking = Booking(**booking_data.dict())
    db.add(booking)
    db.commit()

    return booking
```

### 3.4. Отмена записи

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| Отмена за 24+ часа | Полный возврат (100%) | ⬜ |
| Отмена за 12-24 часа | Частичный возврат (50%) | ⬜ |
| Отмена < 12 часов | Без возврата | ⬜ |
| Статус у салона/мастера | "Отменено клиентом" | ⬜ |
| Уведомления | Обоим сторонам | ⬜ |

### 3.5. История записей

| Проверка | Статус |
|----------|--------|
| Прошлые записи не редактируются | ⬜ |
| Статус читабельный (завершено/отменено/неявка) | ⬜ |
| История сохраняется при удалении пользователя | ⬜ |

### 3.6. Валидация времени

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| Запись на прошлое время | Ошибка "Cannot book past time" | ⬜ |
| Запись в нерабочие часы (салон закрыт) | Ошибка "Salon closed" | ⬜ |
| Запись в выходной день | Ошибка "Salon closed on this day" | ⬜ |
| Запись на время обеда мастера | Ошибка "Master on break" | ⬜ |

**Автоматизированный тест**:
```python
# backend/tests/test_booking_validation.py

def test_cannot_book_past_time():
    response = client.post("/api/bookings", json={
        "start_at": "2020-01-01T10:00:00Z"  # Прошлое
    })
    assert response.status_code == 400
    assert "past" in response.json()["detail"].lower()

def test_cannot_book_closed_hours():
    response = client.post("/api/bookings", json={
        "salon_id": 1,
        "start_at": "2025-12-11T02:00:00Z"  # 2 ночи
    })
    assert response.status_code == 400
    assert "closed" in response.json()["detail"].lower()
```

---

## 4️⃣ ПЛАТЕЖИ (КРИТИЧНО!)

### 4.1. Базовая оплата

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| ✅ Успешная оплата (Payme) | Статус "paid", запись подтверждена | ⬜ |
| ✅ Успешная оплата (Click) | Статус "paid", запись подтверждена | ⬜ |
| ❌ Отмена пользователем | Статус "cancelled", запись отменена | ⬜ |
| ❌ Отказ платёжки (нет средств) | Статус "failed", запись pending | ⬜ |

### 4.2. 🔥 UI не обманывает

**Проверка**: UI **НЕ** показывает "Оплата успешна" до подтверждения от backend!

**Как проверить**:
```
1. Открыть DevTools → Network
2. Начать оплату
3. Поймать момент перед redirect'ом от Payme
4. Проверить: UI показывает "Обработка...", НЕ "Успех"
```

**❌ НЕПРАВИЛЬНО (небезопасно)**:
```typescript
// ❌ НЕ ТАК!
const handlePaymentRedirect = (paymentId: number) => {
    // Пользователь вернулся с Payme → сразу "успех"
    setBookingStatus('paid')  // ОПАСНО!
}
```

**✅ ПРАВИЛЬНО**:
```typescript
// ✅ ТАК!
const handlePaymentRedirect = async (paymentId: number) => {
    // Проверяем статус на сервере
    const response = await api.get(`/api/payments/${paymentId}`)

    if (response.data.status === 'completed') {
        setBookingStatus('paid')
    } else {
        setBookingStatus('pending')
    }
}
```

### 4.3. 🔥 Дубли транзакций

**Сценарий 1**: F5 на странице оплаты

**Проверка**:
```
1. Начать оплату
2. На странице результата нажать F5
3. Проверить БД: только 1 транзакция
```

**Сценарий 2**: Повторный callback от Payme

**Проверка**:
```
1. Отправить callback на /api/payments/payme/callback
2. Отправить ЕЩЁ РАЗ тот же callback
3. Проверить: только 1 транзакция в БД
```

**🛡️ Защита (idempotency)**:
```python
# backend/app/api/payments.py

@router.post("/payme/callback")
def payme_callback(request: PaymeRequest, db: Session = Depends(get_db)):
    # Idempotency key
    idempotency_key = f"payme_{request.id}_{request.method}"

    # Проверяем, не обрабатывали ли мы это раньше
    existing_log = db.query(PaymentAuditLog).filter(
        PaymentAuditLog.idempotency_key == idempotency_key
    ).first()

    if existing_log:
        # Уже обработан - возвращаем прежний ответ
        return PaymeResponse(result={"message": "Already processed"})

    # Обрабатываем...
    # ...

    # Сохраняем audit log с idempotency_key
    audit_log = PaymentAuditLog(
        idempotency_key=idempotency_key,
        ...
    )
    db.add(audit_log)
    db.commit()
```

### 4.4. Логи платежей

| Проверка | Статус |
|----------|--------|
| Любая транзакция → есть запись в `payments` | ⬜ |
| Любой callback → есть запись в `payment_audit_logs` | ⬜ |
| Логи содержат: user_id, amount, status, timestamp | ⬜ |
| Логи содержат: IP, signature validity | ⬜ |

---

## 5️⃣ САЛОНЫ, МАСТЕРА, УСЛУГИ

### 5.1. Создание салона

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| ✅ Все поля заполнены | Салон создан, на модерации | ⬜ |
| ❌ Без названия | Ошибка "Name required" | ⬜ |
| ❌ Без адреса | Ошибка "Address required" | ⬜ |
| ❌ Без телефона | Ошибка "Phone required" | ⬜ |
| ❌ Некорректный телефон | Ошибка "Invalid phone format" | ⬜ |

### 5.2. Загрузка фото

| Сценарий | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| ✅ Фото 1920x1080, JPEG, 2MB | Загружено | ⬜ |
| ❌ Фото > 10MB | Ошибка "File too large" | ⬜ |
| ❌ Файл не изображение (.exe) | Ошибка "Invalid file type" | ⬜ |
| ❌ Испорченный JPEG | Ошибка "Corrupted file" | ⬜ |

**Автоматизированный тест**:
```python
# backend/tests/test_uploads.py

def test_upload_valid_image():
    with open("tests/fixtures/test_image.jpg", "rb") as f:
        response = client.post("/api/uploads", files={
            "file": ("test.jpg", f, "image/jpeg")
        })
    assert response.status_code == 200
    assert "url" in response.json()

def test_upload_too_large_file():
    # 15MB файл
    large_file = io.BytesIO(b"0" * 15 * 1024 * 1024)

    response = client.post("/api/uploads", files={
        "file": ("large.jpg", large_file, "image/jpeg")
    })
    assert response.status_code == 413  # Payload Too Large

def test_upload_invalid_format():
    response = client.post("/api/uploads", files={
        "file": ("virus.exe", io.BytesIO(b"MZ..."), "application/x-msdownload")
    })
    assert response.status_code == 400
```

### 5.3. Услуги

| Проверка | Статус |
|----------|--------|
| Создать услугу с ценой и длительностью | ⬜ |
| Редактировать цену | ⬜ |
| Скрыть услугу (is_active = false) | ⬜ |
| Скрытая услуга НЕ отображается клиентам | ⬜ |
| Скрытую услугу ВИДИТ владелец салона | ⬜ |

### 5.4. Мастера

| Проверка | Статус |
|----------|--------|
| Добавить мастера | ⬜ |
| Назначить услуги мастеру | ⬜ |
| Деактивировать мастера | ⬜ |
| Деактивированный мастер НЕ доступен для записи | ⬜ |

---

## 6️⃣ ПОИСК И ФИЛЬТРЫ

### 6.1. Поиск

| Что ищем | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| По названию салона | Найдены релевантные салоны | ⬜ |
| По адресу | Найдены салоны в этом районе | ⬜ |
| По услуге ("маникюр") | Найдены салоны с этой услугой | ⬜ |

### 6.2. Фильтры

| Фильтр | Проверка | Статус |
|--------|----------|--------|
| Цена (мин-макс) | Только салоны в диапазоне | ⬜ |
| Рейтинг (от 4.0) | Только с рейтингом ≥ 4.0 | ⬜ |
| Ближайшее время | Салоны с доступными слотами | ⬜ |

### 6.3. Комбинации

| Проверка | Статус |
|----------|--------|
| 2 фильтра одновременно (цена + рейтинг) | ⬜ |
| 3 фильтра (цена + рейтинг + услуга) | ⬜ |

### 6.4. Пустые результаты

**Проверка**: Нет салонов → адекватное сообщение

**❌ НЕПРАВИЛЬНО**:
```
null
undefined
[]
```

**✅ ПРАВИЛЬНО**:
```
"Салонов не найдено. Попробуйте изменить фильтры."
```

---

## 7️⃣ УВЕДОМЛЕНИЯ

### 7.1. Email/SMS

| Событие | Кто получает | Статус |
|---------|--------------|--------|
| Создание записи | Клиент + Салон | ⬜ |
| Отмена записи | Клиент + Салон | ⬜ |
| Напоминание (за 1 час) | Клиент | ⬜ |
| Оплата успешна | Клиент + Салон | ⬜ |

### 7.2. Ошибки доставки

| Проверка | Статус |
|----------|--------|
| Notification сервис упал → UI не ломается | ⬜ |
| Ошибка логируется | ⬜ |
| Повторная попытка (retry) через 1 минуту | ⬜ |
| После 3 неудач → Dead Letter Queue | ⬜ |

---

## 8️⃣ ЛИЧНЫЙ КАБИНЕТ КЛИЕНТА

| Проверка | Статус |
|----------|--------|
| Просмотр активных записей | ⬜ |
| Просмотр прошедших записей | ⬜ |
| Изменение имени | ⬜ |
| Изменение телефона | ⬜ |
| Смена языка (RU/UZ/EN) | ⬜ |
| Смена пароля (старый неверный → ошибка) | ⬜ |
| Смена пароля (старый верный → успех) | ⬜ |
| Удаление аккаунта → записи анонимизируются | ⬜ |

---

## 9️⃣ ЛИЧНЫЙ КАБИНЕТ ВЛАДЕЛЬЦА САЛОНА

| Проверка | Статус |
|----------|--------|
| Дашборд: количество записей за день/неделю | ⬜ |
| Ближайшие записи | ⬜ |
| Управление услугами | ⬜ |
| Управление мастерами | ⬜ |
| Статусы записей (подтверждено, завершено, отменено) | ⬜ |
| Возможность отменить запись | ⬜ |
| Возможность перенести (если реализовано) | ⬜ |

---

## 🔟 АДМИН-ПАНЕЛЬ

| Проверка | Статус |
|----------|--------|
| Просмотр всех салонов | ⬜ |
| Просмотр всех пользователей | ⬜ |
| Деактивация салона → пользователи не видят его | ⬜ |
| Деактивация пользователя → показывается сообщение | ⬜ |
| Модерация салонов (описания, фото) | ⬜ |
| Ручное исправление данных | ⬜ |
| Все действия админа → записываются в audit_logs | ⬜ |

---

## 1️⃣1️⃣ UX / UI / УСТРОЙСТВА

### 11.1. Мобильные устройства

| Устройство | Проверка | Статус |
|------------|----------|--------|
| **iPhone SE (320px)** | Всё читаемо, кнопки не наезжают | ⬜ |
| **iPhone 12/13** | Корректное отображение | ⬜ |
| **Samsung Galaxy S21** | Корректное отображение | ⬜ |
| **iPad** | Корректное отображение | ⬜ |

### 11.2. Браузеры

| Браузер | Версия | Статус |
|---------|--------|--------|
| Chrome | Latest | ⬜ |
| Firefox | Latest | ⬜ |
| Safari | Latest | ⬜ |
| Edge | Latest | ⬜ |

### 11.3. State управление

| Проверка | Статус |
|----------|--------|
| Loader'ы есть при ожидании | ⬜ |
| Нет "мертвых" кликов | ⬜ |
| Disabled кнопки при обработке | ⬜ |

### 11.4. Ошибки

**❌ ПЛОХО**:
```
500 Internal Server Error
```

**✅ ХОРОШО**:
```
Что-то пошло не так. Попробуйте ещё раз или свяжитесь с поддержкой.
```

---

## 1️⃣2️⃣ ЛОКАЛИЗАЦИЯ

| Проверка | Статус |
|----------|--------|
| Переключение RU → UZ → EN | ⬜ |
| Текст меняется | ⬜ |
| Плейсхолдеры меняются | ⬜ |
| URL не ломается | ⬜ |
| Нет "микса языков" на одной странице | ⬜ |

---

## 1️⃣3️⃣ НЕГАТИВНЫЕ СЦЕНАРИИ (СПЕЦИАЛЬНО СЛОМАТЬ)

### 13.1. Ввод в поля

| Что вводим | Ожидаемый результат | Статус |
|------------|---------------------|--------|
| Длинная строка (255+ символов) | Обрезается или ошибка | ⬜ |
| SQL injection (`' OR 1=1; DROP TABLE users;--`) | Безопасно игнорируется | ⬜ |
| XSS (`<script>alert(1)</script>`) | Экранируется | ⬜ |
| HTML (`<h1>Hack</h1>`) | Экранируется | ⬜ |
| Emoji (👍💅✨) | Корректно отображается | ⬜ |

### 13.2. Сеть

| Сценарий | Что делать | Ожидаемый результат | Статус |
|----------|-----------|---------------------|--------|
| **Отключить интернет во время сохранения** | DevTools → Offline | Показать ошибку "Нет соединения" | ⬜ |
| **Медленная сеть** | DevTools → Slow 3G | Loader'ы, не зависает | ⬜ |
| **Отключить во время оплаты** | Offline после redirect | Показать ошибку, не создавать запись | ⬜ |

### 13.3. Обновление страницы (F5)

| Где | Ожидаемый результат | Статус |
|-----|---------------------|--------|
| Во время создания записи | Запись НЕ дублируется | ⬜ |
| Во время оплаты | Транзакция НЕ дублируется | ⬜ |
| В личном кабинете | Данные не теряются | ⬜ |

### 13.4. Два браузера

**Проверка**: Открыть один аккаунт в Chrome и Firefox

| Действие | Ожидаемый результат | Статус |
|----------|---------------------|--------|
| Изменить данные в Chrome | Firefox видит изменения после обновления | ⬜ |
| Удалить запись в Chrome | Firefox видит удаление | ⬜ |

---

## 1️⃣4️⃣ API-LEVEL ТЕСТЫ

### 14.1. Все endpoints

| Endpoint | Проверка | Статус |
|----------|----------|--------|
| `GET /api/salons` | 200 OK | ⬜ |
| `GET /api/salons/999` | 404 Not Found | ⬜ |
| `POST /api/bookings` (без токена) | 401 Unauthorized | ⬜ |
| `PATCH /api/salons/2` (чужой салон) | 403 Forbidden | ⬜ |
| `DELETE /api/users/1` (не админ) | 403 Forbidden | ⬜ |

### 14.2. Права доступа

**Матрица**:
```python
# Проверить:
# 1. Клиент НЕ может создать салон
# 2. Владелец НЕ может редактировать чужой салон
# 3. Админ МОЖЕТ всё
```

### 14.3. Rate Limiting

| Endpoint | Limit | Проверка | Статус |
|----------|-------|----------|--------|
| `/api/auth/login` | 5/min | Спамить → 429 Too Many Requests | ⬜ |
| `/api/auth/register` | 3/hour | Спамить → 429 | ⬜ |
| `/api/bookings` | 10/min | Спамить → 429 | ⬜ |

**Автоматизированный тест**:
```python
# backend/tests/test_rate_limiting.py

def test_login_rate_limit():
    # 5 попыток - OK
    for _ in range(5):
        response = client.post("/api/auth/login", json={...})
        assert response.status_code in [200, 401]

    # 6-я попытка - блокировка
    response = client.post("/api/auth/login", json={...})
    assert response.status_code == 429
```

---

## 1️⃣5️⃣ РЕГРЕССИЯ ПЕРЕД ПРОДОМ

**Цель**: Убедиться, что ничего не сломалось перед релизом.

### Чеклист:

- [ ] **Smoke** (5 минут)
- [ ] **Auth + роли** (15 минут)
- [ ] **Запись** - минимум 5 разных сценариев (30 минут)
  - [ ] Базовая запись
  - [ ] Двойной клик
  - [ ] Два пользователя на один слот
  - [ ] Отмена
  - [ ] Валидация времени
- [ ] **Личный кабинет клиента** (10 минут)
- [ ] **Личный кабинет салона** (10 минут)
- [ ] **Поиск + фильтры** (10 минут)
- [ ] **Уведомления** (5 минут)
- [ ] **Платежи** (если есть) (15 минут)

### Full E2E сценарий:

```
1. Регистрация нового пользователя (клиент)
2. Поиск салона по названию
3. Просмотр салона, выбор услуги
4. Создание записи
5. Оплата (если есть)
6. Проверка уведомлений
7. Просмотр записи в личном кабинете
8. Отмена записи
9. Проверка истории
```

**Время**: ~20 минут

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

### Прогресс по категориям:

| Категория | Проверок | Завершено | Прогресс |
|-----------|----------|-----------|----------|
| 1. Smoke | 11 | ⬜ | 0% |
| 2. Авторизация | 15 | ⬜ | 0% |
| 3. Бронирование | 20 | ⬜ | 0% |
| 4. Платежи | 10 | ⬜ | 0% |
| 5. Салоны | 15 | ⬜ | 0% |
| 6. Поиск | 8 | ⬜ | 0% |
| 7. Уведомления | 6 | ⬜ | 0% |
| 8. ЛК Клиента | 8 | ⬜ | 0% |
| 9. ЛК Салона | 7 | ⬜ | 0% |
| 10. Админ | 7 | ⬜ | 0% |
| 11. UX/UI | 10 | ⬜ | 0% |
| 12. Локализация | 5 | ⬜ | 0% |
| 13. Негативные | 12 | ⬜ | 0% |
| 14. API | 10 | ⬜ | 0% |
| 15. Регрессия | 1 | ⬜ | 0% |

**ВСЕГО**: 145 проверок

---

## 🎯 ПРИОРИТИЗАЦИЯ QA

### 🔴 P0: Перед каждым релизом (ОБЯЗАТЕЛЬНО)

1. Smoke-тест
2. Авторизация (регистрация, логин)
3. Бронирование (базовая запись, двойной клик, race condition)
4. Платежи (успех, ошибка, дубли)

**Время**: ~1 час

### 🟠 P1: Перед major релизом

5. Салоны (создание, фото, услуги, мастера)
6. Поиск и фильтры
7. Личные кабинеты (клиент, салон)
8. Уведомления

**Время**: +1.5 часа

### 🟡 P2: Перед production

9. Админ-панель
10. UX/UI на всех устройствах
11. Локализация
12. Негативные сценарии
13. API тесты

**Время**: +2 часа

### 🟢 P3: Continuous

14. Полная регрессия
15. Автоматизированные тесты
16. Performance testing

**Время**: Постоянно

---

## 📝 ШАБЛОН БАГ-РЕПОРТА

```markdown
### Баг: [Краткое описание]

**Приоритет**: P0 / P1 / P2 / P3

**Окружение**:
- OS: Windows 10
- Браузер: Chrome 120
- URL: https://aurelle.uz/client/bookings

**Шаги воспроизведения**:
1. Залогиниться как клиент
2. Выбрать салон "Beauty Studio"
3. Выбрать услугу "Маникюр"
4. Быстро нажать "Записаться" 2 раза

**Ожидаемый результат**:
Создаётся 1 запись

**Фактический результат**:
Создаётся 2 записи

**Логи**:
```
[2025-12-10 10:30:15] POST /api/bookings 200
[2025-12-10 10:30:15] POST /api/bookings 200
```

**Скриншот**: [прикрепить]
**Видео**: [прикрепить]

**Дополнительно**:
БД содержит 2 записи с одинаковым временем для одного мастера
```

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Распечатать этот чеклист** или открыть в отдельной вкладке
2. **Smoke-тест** перед каждым билдом (5 минут)
3. **P0 проверки** перед каждым релизом (1 час)
4. **Полная регрессия** перед production (4.5 часа)
5. **Автоматизировать** критические сценарии (постоянно)

---

**Let's break it!** 🧨💥

*Версия: 1.0.0 | Последнее обновление: 10 декабря 2025*
