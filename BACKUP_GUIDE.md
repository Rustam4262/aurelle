# 💾 Руководство по резервному копированию AURELLE

## 📋 Оглавление

1. [Обзор системы бэкапов](#обзор-системы-бэкапов)
2. [Автоматические бэкапы](#автоматические-бэкапы)
3. [Ручное создание бэкапа](#ручное-создание-бэкапа)
4. [Восстановление из бэкапа](#восстановление-из-бэкапа)
5. [Безопасное обновление](#безопасное-обновление)
6. [Мониторинг бэкапов](#мониторинг-бэкапов)
7. [Решение проблем](#решение-проблем)

---

## 🎯 Обзор системы бэкапов

AURELLE использует **многоуровневую систему резервного копирования** для максимальной защиты данных:

### Уровни бэкапов:

```
📦 backups/
├── 📁 daily/          # Ежедневные бэкапы (7 дней)
│   ├── daily_20240115_030000.sql.gz
│   ├── daily_20240116_030000.sql.gz
│   └── ...
├── 📁 weekly/         # Еженедельные бэкапы (4 недели)
│   ├── weekly_20240107_030000_week01.sql.gz
│   ├── weekly_20240114_030000_week02.sql.gz
│   └── ...
└── 📁 monthly/        # Ежемесячные бэкапы (12 месяцев)
    ├── monthly_20240101_030000_2024-01.sql.gz
    ├── monthly_20240201_030000_2024-02.sql.gz
    └── ...
```

### Политика хранения:

| Тип | Частота | Хранение | Когда создается |
|-----|---------|----------|-----------------|
| **Ежедневные** | Каждый день | 7 дней | Каждое утро в 3:00 |
| **Еженедельные** | Каждое воскресенье | 4 недели | Воскресенье в 3:00 |
| **Ежемесячные** | 1-го числа месяца | 12 месяцев | 1-го числа в 3:00 |

### Что сохраняется:

- ✅ Все пользователи (клиенты, владельцы салонов, администраторы)
- ✅ Все салоны и их данные
- ✅ Все мастера и услуги
- ✅ Все бронирования (прошлые и будущие)
- ✅ Все отзывы и рейтинги
- ✅ Вся история транзакций
- ✅ Все настройки и конфигурации

### Что НЕ сохраняется:

- ❌ Загруженные файлы (фото салонов, логотипы) - хранятся в Docker volumes
- ❌ Логи приложения - хранятся в Docker volumes
- ❌ Кэш Redis - не требует бэкапа

---

## ⚙️ Автоматические бэкапы

### Настройка автоматических бэкапов на сервере

После развёртывания на **aurelle.uz** настройте автоматические бэкапы:

#### 1. Подключитесь к серверу

```bash
ssh aurelle@89.39.94.194
cd ~/projects/aurelle
```

#### 2. Сделайте скрипты исполняемыми

```bash
chmod +x deploy/scripts/*.sh
```

#### 3. Настройте crontab

```bash
crontab -e
```

Добавьте следующие строки:

```cron
# ========================================
# AURELLE - Автоматические бэкапы
# ========================================

# Многоуровневый бэкап каждый день в 3:00 утра
0 3 * * * cd ~/projects/aurelle && bash ./deploy/scripts/advanced_backup.sh >> ~/backups/backup.log 2>&1

# Альтернативно: простой бэкап (если не нужна ротация)
# 0 3 * * * cd ~/projects/aurelle && bash ./deploy/scripts/backup.sh >> ~/backups/backup.log 2>&1

# Очистка логов бэкапов (каждые 30 дней)
0 4 1 * * find ~/backups -name "backup.log" -mtime +30 -delete

# Отправка статистики бэкапов на email (опционально)
# 30 3 * * 0 cd ~/projects/aurelle && bash ./deploy/scripts/backup_report.sh | mail -s "AURELLE Weekly Backup Report" admin@aurelle.uz
```

#### 4. Проверьте настройки crontab

```bash
crontab -l
```

#### 5. Проверьте логи cron

```bash
# Просмотр логов
tail -f ~/backups/backup.log

# Проверка последнего запуска
ls -lah ~/projects/aurelle/backups/daily/ | tail -5
```

---

## 🔧 Ручное создание бэкапа

### Простой бэкап

Создаёт один файл резервной копии:

```bash
cd ~/projects/aurelle
bash ./deploy/scripts/backup.sh
```

**Результат:**
```
./backups/backup_20240115_143022.sql.gz
```

### Многоуровневый бэкап

Создаёт бэкапы с автоматической ротацией:

```bash
cd ~/projects/aurelle
bash ./deploy/scripts/advanced_backup.sh
```

**Результат:**
```
./backups/
├── daily/daily_20240115_143022.sql.gz
├── weekly/weekly_20240114_030000_week02.sql.gz  (если воскресенье)
└── monthly/monthly_20240101_030000_2024-01.sql.gz  (если 1-е число)
```

### Бэкап перед важными операциями

Всегда создавайте бэкап перед:

```bash
# Перед обновлением
bash ./deploy/scripts/backup.sh

# Перед изменением схемы БД
bash ./deploy/scripts/backup.sh

# Перед массовым импортом данных
bash ./deploy/scripts/backup.sh
```

---

## 🔄 Восстановление из бэкапа

### ⚠️ ВНИМАНИЕ

Восстановление из бэкапа **полностью заменяет текущую базу данных**. Все текущие данные будут потеряны!

### Шаг 1: Посмотрите доступные бэкапы

```bash
cd ~/projects/aurelle

# Ежедневные бэкапы
ls -lh ./backups/daily/

# Еженедельные бэкапы
ls -lh ./backups/weekly/

# Ежемесячные бэкапы
ls -lh ./backups/monthly/

# Все бэкапы, отсортированные по дате
find ./backups -name "*.sql.gz" -type f -exec ls -lh {} \; | sort
```

### Шаг 2: Выберите бэкап для восстановления

```bash
# Пример: восстановление из ежедневного бэкапа
BACKUP_FILE="./backups/daily/daily_20240115_030000.sql.gz"

# Проверьте, что файл существует
ls -lh $BACKUP_FILE
```

### Шаг 3: Запустите восстановление

```bash
bash ./deploy/scripts/restore.sh $BACKUP_FILE
```

### Что происходит при восстановлении:

1. ✅ Скрипт запросит подтверждение
2. ✅ Создаст страховочный бэкап текущей БД
3. ✅ Остановит backend (чтобы никто не использовал БД)
4. ✅ Разорвёт все активные подключения к БД
5. ✅ Удалит текущую БД
6. ✅ Создаст новую пустую БД
7. ✅ Восстановит данные из бэкапа
8. ✅ Запустит backend
9. ✅ Проверит, что всё работает

### Пример вывода:

```
[INFO] ============================================
[INFO]   DATABASE RESTORE
[INFO] ============================================
[INFO] Backup file: ./backups/daily/daily_20240115_030000.sql.gz
[INFO] Database: beauty_salon_db
[INFO] Container: aurelle_db_prod
[INFO]
[WARN] ⚠️  WARNING: This will COMPLETELY REPLACE the current database!
[WARN] ⚠️  All current data will be LOST!
[WARN]
[?] Are you sure you want to continue? (yes/no)
yes
[INFO] Creating safety backup of current database...
[INFO] Safety backup created: ./backups/safety_backup_before_restore_20240115_143530.sql.gz
[INFO] Stopping backend service...
[INFO] Terminating active database connections...
[INFO] Dropping existing database...
[INFO] Creating fresh database...
[INFO] Restoring data from backup...
[INFO] ✅ Database restored successfully!
[INFO] Starting backend service...
[INFO] Waiting for backend to be ready...
[INFO] ✅ Backend is healthy!
[INFO] ============================================
[INFO] ✅ RESTORE COMPLETED SUCCESSFULLY!
[INFO] ============================================
```

### Шаг 4: Проверьте работу системы

```bash
# Откройте сайт
open https://aurelle.uz

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs backend | tail -50

# Проверьте статус
docker-compose -f docker-compose.prod.yml ps
```

---

## 🚀 Безопасное обновление

### Использование скрипта update.sh

Скрипт автоматически создаёт бэкап перед обновлением:

```bash
cd ~/projects/aurelle
bash ./deploy/scripts/update.sh
```

### Что делает скрипт update.sh:

1. ✅ Создаёт резервную копию БД
2. ✅ Подтягивает новый код из git
3. ✅ Проверяет здоровье системы
4. ✅ Собирает новые Docker образы
5. ✅ Останавливает старые контейнеры
6. ✅ Запускает новые контейнеры
7. ✅ Выполняет миграции БД
8. ✅ Проверяет, что всё работает

### Откат после неудачного обновления

Если что-то пошло не так:

```bash
# Найдите последний бэкап
ls -lh ./backups/backup_*.sql.gz | tail -1

# Восстановите из него
bash ./deploy/scripts/restore.sh ./backups/backup_XXXXXX.sql.gz
```

---

## 📊 Мониторинг бэкапов

### Проверка статуса бэкапов

```bash
cd ~/projects/aurelle

# Статистика по бэкапам
echo "=== BACKUP STATISTICS ==="
echo "Daily backups: $(find ./backups/daily -name "*.sql.gz" | wc -l)"
echo "Weekly backups: $(find ./backups/weekly -name "*.sql.gz" | wc -l)"
echo "Monthly backups: $(find ./backups/monthly -name "*.sql.gz" | wc -l)"
echo "Total size: $(du -sh ./backups | cut -f1)"

# Последние бэкапы
echo ""
echo "=== LATEST BACKUPS ==="
find ./backups -name "*.sql.gz" -type f -exec ls -lh {} \; | sort -k6,7 | tail -5
```

### Проверка целостности бэкапа

```bash
# Проверка, что файл не повреждён
gunzip -t ./backups/daily/daily_20240115_030000.sql.gz
echo $?  # Должно быть 0 (успех)

# Просмотр содержимого (первые 50 строк)
gunzip -c ./backups/daily/daily_20240115_030000.sql.gz | head -50
```

### Создание отчёта о бэкапах

```bash
# Создайте скрипт для email отчётов
cat > ~/backup_report.sh << 'EOF'
#!/bin/bash
cd ~/projects/aurelle
echo "AURELLE Backup Report - $(date)"
echo "================================"
echo ""
echo "Latest backups:"
find ./backups -name "*.sql.gz" -type f -exec ls -lh {} \; | sort -k6,7 | tail -10
echo ""
echo "Total backup size: $(du -sh ./backups | cut -f1)"
echo "Disk usage: $(df -h . | tail -1)"
EOF

chmod +x ~/backup_report.sh
~/backup_report.sh
```

---

## 🔍 Решение проблем

### Проблема: Бэкап не создаётся

**Симптомы:**
```
[ERROR] Database container is not running!
```

**Решение:**
```bash
# Проверьте статус контейнера
docker ps | grep aurelle_db_prod

# Если не запущен, запустите
docker-compose -f docker-compose.prod.yml up -d postgres

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs postgres
```

### Проблема: Не хватает места на диске

**Симптомы:**
```
[ERROR] No space left on device
```

**Решение:**
```bash
# Проверьте использование диска
df -h

# Очистите старые бэкапы вручную
find ./backups/daily -name "*.sql.gz" -mtime +7 -delete

# Очистите Docker
docker system prune -a -f

# Очистите логи
sudo journalctl --vacuum-size=100M
```

### Проблема: Восстановление зависло

**Симптомы:**
- Скрипт не завершается
- База данных недоступна

**Решение:**
```bash
# Остановите скрипт (Ctrl+C)

# Запустите контейнеры заново
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs postgres
docker-compose -f docker-compose.prod.yml logs backend

# Попробуйте восстановление снова
bash ./deploy/scripts/restore.sh ./backups/backup_XXXXX.sql.gz
```

### Проблема: Бэкап повреждён

**Симптомы:**
```
gunzip: backup.sql.gz: invalid compressed data--format violated
```

**Решение:**
```bash
# Попробуйте предыдущий бэкап
ls -lt ./backups/daily/*.sql.gz | head -5

# Восстановите из более раннего бэкапа
bash ./deploy/scripts/restore.sh ./backups/daily/daily_YYYYMMDD_HHMMSS.sql.gz

# На будущее: включите проверку целостности
echo "0 5 * * * gunzip -t ~/projects/aurelle/backups/daily/*.sql.gz" | crontab -
```

---

## 📝 Чек-лист администратора

### Ежедневно:
- ✅ Проверить, что автоматический бэкап выполнился (проверить логи)
- ✅ Проверить использование диска

### Еженедельно:
- ✅ Просмотреть список всех бэкапов
- ✅ Проверить целостность последнего бэкапа
- ✅ Проверить, что еженедельный бэкап создался (в воскресенье)

### Ежемесячно:
- ✅ Проверить, что месячный бэкап создался (1-го числа)
- ✅ Скачать месячный бэкап на внешний носитель
- ✅ Протестировать восстановление на тестовом сервере

### Перед обновлением:
- ✅ Создать ручной бэкап
- ✅ Убедиться, что есть свободное место
- ✅ Использовать `update.sh` для безопасного обновления
- ✅ Проверить работу системы после обновления

---

## 🎯 Рекомендации

### Защита данных на уровне PRO

1. **Удалённое хранилище** (рекомендуется для production):
   ```bash
   # Синхронизация бэкапов на удалённый сервер
   rsync -avz --progress ./backups/ backup-server:/backups/aurelle/

   # Или в облако (AWS S3, Yandex Object Storage)
   # Настройте в .env файле
   ```

2. **Шифрование бэкапов** (для чувствительных данных):
   ```bash
   # Зашифровать бэкап
   gpg --symmetric --cipher-algo AES256 backup.sql.gz

   # Расшифровать
   gpg --decrypt backup.sql.gz.gpg > backup.sql.gz
   ```

3. **Тестирование восстановления**:
   - Раз в месяц проверяйте восстановление на тестовом окружении
   - Замеряйте время восстановления
   - Документируйте процесс

4. **Мониторинг**:
   - Настройте уведомления при сбое бэкапа
   - Используйте Telegram bot для отчётов
   - Логируйте все операции с бэкапами

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose -f docker-compose.prod.yml logs`
2. Проверьте документацию: [README.md](README.md)
3. Проверьте статус системы: `docker-compose -f docker-compose.prod.yml ps`

**Важные файлы:**
- Бэкапы: `./backups/`
- Логи бэкапов: `~/backups/backup.log`
- Конфигурация: `.env`
- Скрипты: `./deploy/scripts/`

---

**Помните**: Бэкапы — это ваша страховка! Регулярно проверяйте, что они создаются и работают. 💾✅
