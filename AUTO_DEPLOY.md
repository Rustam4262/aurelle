# 🚀 Автоматический деплой

Я подготовил скрипт для автоматического деплоя, но для его работы нужен пароль от базы данных.

## ⚠️ Важно перед запуском:

**Вам нужен пароль от базы данных:**
- База: `aurelleu_aurelle_db`
- Пользователь: `aurelleu_aurelle_user`
- Пароль: **узнайте у администратора сервера или в панели управления хостингом**

## 🎯 Варианты деплоя:

### Вариант 1: Автоматический скрипт (Windows PowerShell)

```powershell
# Запустите скрипт
.\deploy-production.ps1
```

Скрипт автоматически:
1. ✅ Проверит подключение к серверу
2. ✅ Установит Docker (если нужно)
3. ✅ Загрузит файлы проекта
4. ✅ Создаст .env файл (попросит ввести пароль БД)
5. ✅ Запустит деплой
6. ✅ Проверит статус

### Вариант 2: Ручной деплой (если скрипт не работает)

Следуйте инструкциям в **README_DEPLOY.md**

---

## 📋 Что делает скрипт:

1. **Проверяет SSH подключение** к серверу
2. **Устанавливает Docker** (если не установлен)
3. **Загружает проект** на сервер (используя rsync или scp)
4. **Создает .env файл** с настройками (попросит ввести пароль БД)
5. **Запускает деплой** через deploy.sh
6. **Проверяет статус** контейнеров

---

## 🔧 Требования:

- Windows PowerShell
- SSH клиент (обычно встроен в Windows 10+)
- Доступ к серверу (SSH ключ или пароль)

---

## ❓ Если скрипт не работает:

1. **Проверьте SSH подключение вручную:**
   ```bash
   ssh root@89.39.94.194
   ```

2. **Если нужна настройка SSH ключа:**
   ```powershell
   # Сгенерировать ключ (если нет)
   ssh-keygen -t rsa -b 4096
   
   # Скопировать на сервер
   type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@89.39.94.194 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
   ```

3. **Используйте ручной деплой** (см. README_DEPLOY.md)

---

## 🚀 Запуск:

Просто запустите скрипт и следуйте инструкциям:

```powershell
.\deploy-production.ps1
```

**Скрипт сам все сделает!** ✨

